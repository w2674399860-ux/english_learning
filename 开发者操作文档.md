# AI English Learning App — 开发者操作文档

## 一、项目概述

一款基于 AI 的英语学习 App，核心功能：

- 用户上传图片（手写英文、课本、截图等）
- AI 自动识别图片中的英文单词
- 自动生成包含这些单词的英文短文
- 自动翻译为中文
- 自动生成中英文填空练习
- 支持保存学习记录、查看历史

### 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | Flutter 3.41.7 (Dart 3.11.5) | Web/移动端跨平台 |
| 后端 | FastAPI 0.115.0 (Python 3.13) | RESTful API 服务 |
| OCR | PaddleOCR (Docker) | 图片文字识别 |
| AI | DeepSeek API | 短文生成、翻译、填空 |
| 数据库 | SQLite（本地开发） | 学习记录存储 |
| HTTP | Dio 5.9 (Flutter) / httpx (Python) | API 通信 |

---

## 二、开发环境

### 2.1 软件清单

以下软件已安装配置完毕：

| # | 软件 | 版本 | 安装路径 |
|---|------|------|----------|
| 1 | Flutter SDK | 3.41.7 | `D:\code_software\flutter_SDK\flutter` |
| 2 | Dart SDK | 3.11.5 | 随 Flutter 自带 |
| 3 | Python | 3.13.4 | `D:\all_of_python\python3134\python.exe` |
| 4 | pip | 最新 | 随 Python 自带 |
| 5 | Docker | 29.4.0 | `C:\Program Files\Docker\Docker` |
| 6 | Docker Compose | 最新 | 随 Docker 自带 |
| 7 | Git | 2.53.0 | `D:\code_software\git\Git` |
| 8 | Android Studio | — | Flutter Android 构建用 |
| 9 | VS Code | 1.118.1 | 代码编辑器 |
| 10 | Java JDK | 25.0.2 | `D:\all_of_java\JDK` |

### 2.2 端口规划

| 服务 | 端口 | 说明 |
|------|------|------|
| Flutter App（开发模式） | 随机端口 | `flutter run -d chrome` 时自动分配 |
| Flutter Web（生产构建） | 8002 | 由 FastAPI 托管，同源访问 |
| FastAPI 后端 | 8002 | API 服务 + 静态文件托管 |
| PaddleOCR | 8866 | Docker OCR 识别服务 |
| DeepSeek API | — | 云端 AI 服务，不需要本地端口 |

---

## 三、项目目录结构

```
english_learning_app/
│
├── .env                          # 根环境变量（API Key 备用）
├── .gitignore                    # Git 忽略规则
├── README.md                     # 项目简介
├── 开发者操作文档.md               # ← 本文档
│
├── frontend_flutter/             # Flutter 前端项目
│   ├── pubspec.yaml              # Flutter 依赖配置
│   ├── analysis_options.yaml     # Dart 静态分析规则
│   ├── lib/
│   │   ├── main.dart             # ★ 入口文件（Provider + MaterialApp + 底部导航）
│   │   │
│   │   ├── config/
│   │   │   └── api_config.dart   # ★ API 配置（baseUrl、端点、超时）
│   │   │
│   │   ├── models/
│   │   │   └── learning_record.dart  # 学习记录数据模型
│   │   │
│   │   ├── services/
│   │   │   └── api_service.dart  # ★ HTTP 客户端（Dio 封装）
│   │   │
│   │   ├── providers/
│   │   │   └── app_provider.dart # ★ 全局状态管理（Provider）
│   │   │
│   │   └── pages/
│   │       ├── home/
│   │       │   └── home_page.dart    # 首页（相册/拍照上传）
│   │       ├── ocr/
│   │       │   └── ocr_page.dart     # OCR 识别页（loading/error/自动跳转）
│   │       ├── story/
│   │       │   └── story_page.dart   # 四格学习页（原文/翻译/英文填空/中文填空）
│   │       └── history/
│   │           └── history_page.dart # 历史记录列表
│   │
│   └── test/
│       └── widget_test.dart      # Flutter 测试
│
├── backend_fastapi/              # FastAPI 后端项目
│   ├── .env                      # ★ 后端环境变量（API Key 在此配置）
│   ├── main.py                   # ★ FastAPI 入口（路由 + CORS + 静态文件托管）
│   ├── requirements.txt          # Python 依赖列表
│   ├── static/                   # Flutter Web 构建产物（自动生成）
│   │   └── ...                   # flutter build web 的输出
│   ├── data/
│   │   └── english_learning.db   # SQLite 数据库文件（自动创建）
│   │
│   └── app/
│       ├── __init__.py
│       │
│       ├── core/
│       │   ├── __init__.py
│       │   └── config.py         # ★ 配置管理（pydantic-settings，读取 .env）
│       │
│       ├── api/
│       │   ├── __init__.py
│       │   ├── router.py         # 路由汇总（/api/v1/...）
│       │   ├── ocr.py            # OCR 识别接口
│       │   ├── story.py          # 故事生成 + 填空生成接口
│       │   └── history.py        # 学习记录 CRUD 接口
│       │
│       ├── services/
│       │   ├── __init__.py
│       │   ├── ocr_service.py    # ★ OCR 服务（Docker OCR + Mock 降级）
│       │   └── ai_service.py     # ★ AI 服务（DeepSeek API + Mock 降级）
│       │
│       ├── models/
│       │   ├── __init__.py
│       │   └── history.py        # SQLite 数据库操作
│       │
│       └── schemas/
│           ├── __init__.py
│           └── history.py        # Pydantic 请求/响应模型
│
├── docker/
│   └── docker-compose.yml        # PaddleOCR Docker 配置
│
└── docs/                         # 文档目录（预留）
```

---

## 四、启动指南

### 4.1 快速启动（开发模式）

后端和前端可以分别独立启动，也可以让后端直接托管前端。

#### 方式一：后端托管前端（推荐，无跨域问题）

```bash
# 1. 构建 Flutter Web 产物
cd frontend_flutter
flutter build web --release

# 2. 复制到后端静态目录（自动完成，首次需手动）
#    构建产物在 frontend_flutter/build/web/
#    复制到 backend_fastapi/static/

# 3. 启动后端（自动托管前端页面）
cd ../backend_fastapi
python -m uvicorn main:app --host 0.0.0.0 --port 8002
```

访问 `http://localhost:8002` 即可使用完整应用。

#### 方式二：前后端分离开发

```bash
# 终端1：启动后端
cd backend_fastapi
python -m uvicorn main:app --host 0.0.0.0 --port 8002 --reload

# 终端2：启动 Flutter 开发模式
cd frontend_flutter
flutter run -d chrome
```

访问 Flutter 控制台输出的地址（如 `http://localhost:随机端口`）。

### 4.2 验证服务状态

```bash
# 健康检查
curl http://localhost:8002/health
# 返回: {"status":"ok","version":"1.0.0"}

# OCR 接口测试
curl -X POST http://localhost:8002/api/v1/ocr/recognize -F "file=@test.jpg"

# 故事生成测试
curl -X POST http://localhost:8002/api/v1/story/generate \
  -H "Content-Type: application/json" \
  -d '{"words":["apple","book","cat"],"difficulty":"intermediate"}'

# 填空生成测试
curl -X POST http://localhost:8002/api/v1/story/fill-blank \
  -H "Content-Type: application/json" \
  -d '{"english":"The cat likes apples.","chinese":"猫喜欢苹果。"}'
```

---

## 五、配置说明

### 5.1 环境变量文件

项目中有两个 `.env` 文件：

#### `backend_fastapi/.env`（后端实际读取）

```ini
# DeepSeek API 配置（必填）
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DEEPSEEK_API_BASE=https://api.deepseek.com

# 数据库
DATABASE_URL=sqlite:///./data/english_learning.db

# 服务器
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
DEBUG=true

# OCR 服务
OCR_SERVICE_URL=http://localhost:8866
FRONTEND_URL=http://localhost:3000
```

#### `english_learning_app/.env`（根目录，备用）

仅作为参考备份，后端实际读取的是 `backend_fastapi/.env`。

### 5.2 配置加载机制

`app/core/config.py` 使用 `pydantic-settings` 读取环境变量：

```python
class Settings(BaseSettings):
    deepseek_api_key: str = ""
    ocr_mode: str = "auto"  # "docker" / "mock" / "auto"
    ...
    model_config = {"env_file": ".env", "env_file_encoding": "utf-8"}
```

**关键点**：`env_file` 路径是相对于**当前工作目录**（启动命令的目录），所以需要在 `backend_fastapi/` 目录下启动服务。

### 5.3 OCR 模式配置

在 `backend_fastapi/.env` 中设置：

| OCR_MODE 值 | 行为 |
|-------------|------|
| `auto`（默认） | 优先尝试 Docker OCR，失败则自动降级为 Mock |
| `docker` | 仅使用 Docker OCR，不可用时返回错误 |
| `mock` | 直接使用 Mock 模式，返回示例单词 |

---

## 六、API 接口文档

所有 API 前缀：`/api/v1`

### 6.1 OCR 识别

```
POST /api/v1/ocr/recognize
Content-Type: multipart/form-data

参数：
  file: 图片文件（jpg/png 等）

成功响应 (200)：
{
  "words": ["apple", "book", "cat", "dog", ...]
}

错误响应 (503)：
{
  "detail": "OCR service is not running..."
}
```

### 6.2 英文短文生成

```
POST /api/v1/story/generate
Content-Type: application/json

请求体：
{
  "words": ["apple", "book", "cat"],
  "difficulty": "intermediate"   // beginner | intermediate | advanced
}

成功响应 (200)：
{
  "english": "The cat had been eyeing the apple on the table...",
  "chinese": "那只猫盯着桌上的苹果已经好几个小时了..."
}
```

### 6.3 中英文填空生成

```
POST /api/v1/story/fill-blank
Content-Type: application/json

请求体：
{
  "english": "The cat likes apples.",
  "chinese": "猫喜欢苹果。"
}

成功响应 (200)：
{
  "english_blank": "The ___ likes ___.",
  "chinese_blank": "___ 喜欢 ___。"
}
```

### 6.4 保存学习记录

```
POST /api/v1/history/save
Content-Type: application/json

请求体：
{
  "image_url": "photo.jpg",
  "words": ["apple", "book"],
  "english_story": "...",
  "chinese_translation": "...",
  "english_blank": "...",
  "chinese_blank": "..."
}

成功响应 (200)：
{
  "id": 1,
  "message": "Record saved successfully"
}
```

### 6.5 获取历史记录列表

```
GET /api/v1/history/records?page=1&page_size=20&search=关键词

成功响应 (200)：
{
  "records": [...],
  "total": 5,
  "page": 1,
  "page_size": 20
}
```

### 6.6 获取单条记录

```
GET /api/v1/history/records/{record_id}

成功响应 (200)：单条记录对象
不存在 (404)：{"detail": "Record not found"}
```

### 6.7 更新记录

```
PUT /api/v1/history/records/{record_id}
Content-Type: application/json

请求体：
{
  "is_favorite": true,
  "notes": "值得复习"
}
```

### 6.8 删除记录

```
DELETE /api/v1/history/records/{record_id}

成功响应 (200)：{"message": "Record deleted successfully"}
```

---

## 七、应用数据流

用户操作 → 前端 → 后端 → AI 服务的完整流程：

```
用户上传图片
     │
     ▼
Flutter HomePage
  → image_picker 选择图片
  → 读取图片字节 (readAsBytes)
  → 跳转到 OCRPage
     │
     ▼
Flutter OCRPage
  → POST /api/v1/ocr/recognize (MultipartFile 上传)
     │
     ▼
FastAPI OCR 接口
  → OCRService.recognize(image_data)
  → 尝试 Docker OCR (localhost:8866)
  → 失败则 Mock OCR (返回示例单词)
     │
     ▼
Flutter Provider.recognizeText()
  → 收到单词列表
  → POST /api/v1/story/generate
     │
     ▼
FastAPI Story 接口
  → AIService.generate_story(words)
  → 检查 API Key → 有 Key 则调 DeepSeek API
  → 无 Key 则返回 Mock 故事
     │
     ▼
Flutter Provider._generateStory()
  → 收到英文故事 + 中文翻译
  → POST /api/v1/story/fill-blank
     │
     ▼
FastAPI FillBlank 接口
  → AIService.generate_fill_blank(english, chinese)
  → 同上，调 DeepSeek 或 Mock
     │
     ▼
Flutter Provider
  → 构建 LearningRecord
  → 跳转到 StoryPage（四格展示）
     │
     ▼
用户查看学习结果
  → 英文原文 | 中文翻译 | 英文填空 | 中文填空
  → 点击保存 → POST /api/v1/history/save
```

---

## 八、常见问题排查

### 8.1 启动相关

**Q: 端口被占用，启动失败**
```
Error: [WinError 10048] 端口已被占用
```
解决：查找占用进程并终止
```bash
netstat -ano | findstr :8002
taskkill /f /pid <PID>
```

**Q: Python 模块未安装**
```
ModuleNotFoundError: No module named 'fastapi'
```
解决：
```bash
cd backend_fastapi
pip install -r requirements.txt
```

### 8.2 OCR 相关

**Q: 上传图片后返回 503 "OCR service is not running"**
原因：PaddleOCR Docker 容器未启动。
解决：启动 Docker Desktop，然后运行：
```bash
cd docker
docker compose up -d
```

**Q: 返回 500 "Internal Server Error"**
原因：后端代码异常崩溃。
解决：查看后端终端窗口的具体错误堆栈，检查：
1. `.env` 配置是否正确（API Key 等）
2. OCR 服务是否可连接
3. DeepSeek API 是否可访问

**Q: OCR 一直返回示例单词（mock 模式）**
原因：`OCR_MODE` 设为 `auto` 且 Docker OCR 不可用，自动降级。
解决：启动 Docker OCR，或在 `.env` 中设 `OCR_MODE=docker` 可看到明确错误。

### 8.3 AI 生成相关

**Q: 故事生成返回 Mock 数据（不是 AI 生成的内容）**
原因：DeepSeek API Key 未配置或无效。
解决：检查 `backend_fastapi/.env` 中的 `DEEPSEEK_API_KEY` 是否正确。

**Q: 故事生成返回 500**
原因：DeepSeek API 调用失败（网络问题/Key 过期/余额不足）。
解决：检查后端终端中的具体错误信息。

### 8.4 前端相关

**Q: "MultipartFile is only supported where dart:io is available"**
原因：Flutter Web 不支持 `dart:io` 的 File API。
解决：已修复，使用 `MultipartFile.fromBytes()` + `XFile.readAsBytes()` 代替。

**Q: "XMLHttpRequest onError callback was called"**
原因：浏览器 CORS 跨域拦截。
解决：使用方案一（后端托管前端）访问 `http://localhost:8002`，或确保后端 CORS 配置为 `allow_origins=["*"]`。

**Q: Flutter 页面白屏/内容不显示**
原因：浏览器缓存旧的预检响应。
解决：关闭所有 Chrome 窗口重新打开，或使用无痕模式。

---

## 九、构建与部署

### 9.1 构建 Flutter Web 生产版本

```bash
cd frontend_flutter
flutter build web --release
```

产物位置：`frontend_flutter/build/web/`

### 9.2 部署到后端

```bash
# 删除旧的静态文件
rm -rf backend_fastapi/static

# 复制新构建
cp -r frontend_flutter/build/web backend_fastapi/static
```

### 9.3 关于 Docker OCR

OCR 服务已通过本地 Dockerfile 构建，无需依赖远程镜像。详见 11.2 节。

构建方式：
```bash
cd docker
docker compose build    # 首次构建
docker compose up -d    # 启动容器
```

### 9.4 Android / iOS 构建

```bash
# Android APK
cd frontend_flutter
flutter build apk --debug

# iOS（需 macOS）
flutter build ios --debug
```

---

## 十、开发规范

### 代码风格
- Python：遵循 PEP 8
- Dart/Flutter：遵循 `flutter_lints` 默认规则
- 运行 `flutter analyze` 确保无警告

### API 设计
- 统一前缀 `/api/v1`
- 所有请求/响应使用 JSON
- API Key 不写死在代码中，通过 `.env` 配置
- 错误处理：接口层捕获业务异常，返回适当 HTTP 状态码

### Git 提交
- 不在当前会话中提交代码（除非用户明确要求）
- 不修改 `.gitconfig` 配置

---

## 十一、VS Code 启动步骤

以下步骤针对 VS Code 环境，使用 VS Code 内置终端启动全部服务。

### 11.1 准备工作

确保以下软件已打开：

1. **Docker Desktop** — 必须提前启动（任务栏搜索 "Docker Desktop" 并打开）
2. **VS Code** — 以项目根目录 `english_learning_app/` 打开

在 VS Code 中打开终端（`` Ctrl + ` ``），以下命令均在 VS Code 终端中执行。

### 11.2 配置与启动 OCR 服务

OCR 服务基于 PaddleOCR，通过 Docker 部署，对外提供 HTTP API（端口 8866），供后端调用进行图片文字识别。

#### 11.2.1 文件结构

```
docker/
├── docker-compose.yml       # Docker Compose 配置
├── data/                    # 数据卷（挂载到容器）
└── paddle_ocr/              # OCR 服务镜像构建目录
    ├── Dockerfile           # 镜像构建文件
    └── server.py            # OCR HTTP 服务（FastAPI + PaddleOCR）
```

#### 11.2.2 构建原理

`docker-compose.yml` 不再依赖远程镜像，而是使用本地 `Dockerfile` 构建：

```yaml
services:
  paddle_ocr:
    build:
      context: ./paddle_ocr    # 使用本地 Dockerfile 构建
      dockerfile: Dockerfile
    image: paddle_ocr_service:latest
    container_name: english_ocr
    ports:
      - "8866:8866"            # 宿主机 8866 → 容器 8866
    restart: unless-stopped
```

`Dockerfile` 基于已下载的 PaddlePaddle 基础镜像，安装 PaddleOCR 并启动 HTTP 服务：

```dockerfile
FROM registry.baidubce.com/paddlepaddle/paddle:latest
RUN pip install paddleocr fastapi uvicorn python-multipart
COPY server.py /app/server.py
WORKDIR /app
CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8866"]
```

`server.py` 提供 `/ocr` 接口，接收图片 multipart 上传，调用 PaddleOCR 识别后返回 JSON：

```
请求：POST /ocr  (multipart/form-data, field: file)
响应：{"data": [{"text": "hello"}, {"text": "world"}, ...]}
```

#### 11.2.3 首次构建（耗时较长，仅需一次）

在 VS Code 终端 **1** 中执行：

```bash
# 进入 docker 目录
cd docker

# 构建镜像（下载 PaddleOCR 依赖，首次约 5-15 分钟）
docker compose build

# 启动容器（后台运行）
docker compose up -d
```

> **注意**：首次构建需要下载 PaddleOCR Python 包及其依赖（约几百 MB），请确保网络通畅。
> 如构建速度慢，可在 `Dockerfile` 的 `pip install` 后添加国内镜像源 `-i https://pypi.tuna.tsinghua.edu.cn/simple`（已默认添加）。

#### 11.2.4 日常启动

镜像构建完成后，后续只需：

```bash
cd docker
docker compose up -d
```

#### 11.2.5 验证 OCR 服务

```bash
# 查看容器运行状态
docker ps

# 查看容器日志
docker compose logs -f

# 健康检查
curl http://localhost:8866/health
# 返回: {"status":"ok","service":"paddle_ocr"}

# 测试 OCR 识别（替换为实际图片路径）
curl -X POST http://localhost:8866/ocr \
  -F "file=@test.jpg"
# 返回: {"data":[{"text":"..."},{"text":"..."}]}
```

#### 11.2.6 常见问题

**Q: 构建失败，提示 `pip install paddleocr` 超时**
原因：PaddleOCR 包较大，网络不稳定。
解决：已默认配置清华镜像源，如仍失败可尝试设置 Docker 代理：
```bash
# 在 Docker Desktop → Settings → Resources → Proxies 中配置
```

**Q: 容器启动后又立即退出**
```bash
# 查看具体错误
docker compose logs
```
常见原因：
1. 端口 8866 被占用 — 修改 `docker-compose.yml` 中的 ports 映射
2. 内存不足 — PaddleOCR 需要至少 2GB 可用内存

**Q: `docker compose build` 提示 `no such file or directory`**
解决：确保在 `docker/` 目录下执行命令，`Dockerfile` 路径为 `./paddle_ocr/Dockerfile`。

**Q: 识别结果为英文单词，但希望识别更多语言**
修改 `server.py` 中的 `PaddleOCR(use_angle_cls=True, lang="en")`，将 `lang` 改为 `ch`（中英文混合）或 `japan` 等。

### 11.3 启动后端

在 VS Code 终端 **2** 中执行：

```bash
# 进入后端目录
cd backend_fastapi

# 安装依赖（首次启动或依赖变更时执行）
pip install -r requirements.txt

# 启动 FastAPI 服务（开发模式，--reload 支持代码变更自动重启）
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

验证：
- 打开浏览器访问 `http://localhost:8000/health`，应返回 `{"status":"ok","version":"1.0.0"}`
- 访问 `http://localhost:8000/docs` 可查看 Swagger API 文档

### 11.4 启动前端（Flutter Web 开发模式）

在 VS Code 终端 **3** 中执行：

```bash
# 进入前端目录
cd frontend_flutter

# 安装依赖（首次启动或 pubspec.yaml 变更时执行）
flutter pub get

# 启动 Flutter Web 开发模式
flutter run -d chrome
```

启动后 Flutter 会自动打开 Chrome 浏览器，终端会显示调试地址（如 `ws://127.0.0.1:54395/...`）。

**Flutter 常用快捷键**（在终端中按对应键）：
| 按键 | 功能 |
|------|------|
| `r` | 热重载（Hot Reload） |
| `R` | 热重启（Hot Restart） |
| `d` | 分离（保持应用运行，退出终端控制） |
| `q` | 退出 |

### 11.5 使用 VS Code Launch 配置（进阶）

创建 `.vscode/launch.json` 文件（在项目根目录）：

```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Backend (FastAPI)",
            "type": "python",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "main:app",
                "--host", "0.0.0.0",
                "--port", "8000",
                "--reload"
            ],
            "cwd": "${workspaceFolder}/backend_fastapi",
            "envFile": "${workspaceFolder}/backend_fastapi/.env"
        }
    ]
}
```

然后按 `F5` 即可直接启动后端调试模式，支持断点。

### 11.6 VS Code 多终端布局参考

```
┌─────────────────────────────────────────────────┐
│  终端 1 (OCR)       │  终端 2 (Backend)          │
│  docker compose up  │  uvicorn main:app --reload │
│                     │                            │
├─────────────────────┴────────────────────────────┤
│  终端 3 (Flutter)                                 │
│  flutter run -d chrome                            │
└──────────────────────────────────────────────────┘
```

VS Code 终端分屏快捷键：
- 垂直拆分：`Ctrl + Shift + 5`
- 切换终端：`Ctrl + `` `
- 新建终端：`Ctrl + Shift + `` `

### 11.7 验证全部服务

```bash
# 后端健康检查
curl http://localhost:8000/health

# OCR 接口测试（需 OCR 容器运行中）
curl -X POST http://localhost:8000/api/v1/ocr/recognize -F "file=@test.jpg"

# API 文档
# 浏览器打开 http://localhost:8000/docs
```

### 11.8 常见 VS Code 问题

**Q: Python 模块未找到（ModuleNotFoundError）**
```
ModuleNotFoundError: No module named 'fastapi'
```
解决：确保在 `backend_fastapi/` 目录下执行了 `pip install -r requirements.txt`

**Q: Flutter 命令未找到**
```
bash: flutter: command not found
```
解决：确保 Flutter SDK 已添加到系统 PATH，或重启 VS Code 使 PATH 变更生效

**Q: 端口被占用**
```
Error: [WinError 10048] 端口已被占用
```
解决：`netstat -ano | findstr :8000` 查找 PID，然后用 `taskkill /f /pid <PID>` 终止

**Q: Flutter Web 白屏**
原因：浏览器缓存问题。关闭所有 Chrome 窗口后重试，或使用无痕模式。

---

## 十二、端口对应速查表

| 访问地址 | 内容 | 启动方式 |
|----------|------|----------|
| `http://localhost:8002` | 完整应用（推荐） | FastAPI 托管 Flutter Web |
| `http://localhost:8002/docs` | API 交互文档 | FastAPI 自动生成（Swagger UI） |
| `http://localhost:8002/health` | 健康检查 | FastAPI 内置 |
| `http://localhost:8866` | PaddleOCR | Docker 容器 |


## 十三、最终的操作步骤
    第一步：启动“动力源” —— Docker Desktop
    这是所有 AI 功能的基础。

  手动打开 Docker Desktop 软件。

  等待左下角的小圆圈变成绿色，并显示 Engine running。

  第二步：启动“识别引擎” —— Docker AI 服务
  在 VS Code 中打开一个新终端（Terminal）。

  进入 Docker 目录：

  <!-- code
  Bash -->
  cd docker
  执行启动命令（确保使用了最新的 RapidOCR 配置）：

  <!-- code
  Bash -->
  docker compose up -d
  验证： 在浏览器打开 http://localhost:8866/health。

  看到 {"status": "ok", "service": "rapid_ocr"} 即可。

  第三步：启动“中枢大脑” —— FastAPI 后端
  这是连接 AI 和前端的桥梁。

  在 VS Code 中打开第二个终端标签页。

  进入后端目录：
<!-- 
  code
  Bash -->
  cd backend_fastapi
  激活 Python 3.12 虚拟环境：

  <!-- code
  Bash -->
  .\venv\Scripts\activate
  启动后端服务（锁定 8002 端口）：

  <!-- code
  Bash -->
  python -m uvicorn main:app --host 0.0.0.0 --port 8002 --reload
  验证： 在浏览器打开 http://localhost:8002/health。

  看到 {"status": "ok"} 即可。

  第四步：启动“展示窗口” —— Flutter 前端
  这是你最终操作的界面。

  在 VS Code 中打开第三个终端标签页。

  进入前端目录：

  <!-- code
  Bash -->
  cd frontend_flutter
  启动 Flutter（以浏览器模式为例）：

  <!-- code
  Bash -->
  flutter run -d chrome